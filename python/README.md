# LinkForge

A URL shortener built with FastAPI, Pydantic, SQLAlchemy, and Alembic.

## Architecture

<!-- Mermaid diagram goes here. -->

## Getting started

1. Copy `.env.example` to `.env` and adjust values.
2. Install dependencies: `pip install -e ".[dev]"`
3. Run the app: `uvicorn app.main:app --reload`
4. Run tests: `pytest`
